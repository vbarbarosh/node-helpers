src/parse_http_content_range.js

Consider replacing first/last by start/end.

The following node apis use start/end names:

fs.createReadStream('a.bin', {start: 100, end: 200})
fs.createWriteStream('a.bin', {start: 100});

- запускать тесты в браузере | add tests to the docs
- создавать "sandboxes" по нажатию кнокпи
