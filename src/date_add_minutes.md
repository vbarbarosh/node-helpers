Adds `minutes` minutes to a `Date` via `setMinutes`. **Mutates the passed
`Date` in place and returns the same instance** — clone first if the original
must be preserved. Negative values subtract; overflow rolls over into hours and
beyond.
