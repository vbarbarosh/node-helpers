Formats a `Date` as `YYYY/MM/DD HH:MM:SS` in local time, all fields
zero-padded to two digits. Same output as `format_date_human`, which is the
more readable implementation.

```js
format_date(new Date('2026/07/06 09:05:03'))    // '2026/07/06 09:05:03'
```
